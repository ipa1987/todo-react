// TaskArea.tsx
import { FC, ReactElement } from 'react';

const TaskArea: FC = (): ReactElement => {
  return (
    <div className="col-md-8 px-4 content-area">
      <h2>Content Area</h2>
    </div>
  );
};

export default TaskArea;
